package HuffManager;


/**
 * The class that serves as an entry point to the whole Program. It reads the parameters which has been passed
 * from the input and creates new GUI elements accordingly.
 *
 * @author Jan Bryda
 */
public class Program {

    public static void main(String[] args) {
        System.out.println(-1 >>> 1);
        System.out.println(Integer.MAX_VALUE);
    }
}
